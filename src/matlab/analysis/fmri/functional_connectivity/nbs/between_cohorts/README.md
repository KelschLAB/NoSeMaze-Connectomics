# Between-cohort NBS

Conditioning-versus-control NBS comparisons are implemented in the
corresponding manuscript figure scripts.

The figure-level code defines the exact unpaired design, contrast,
threshold/permutation settings, component extraction, and plotting.

This folder is therefore a documentation pointer only; no separate
between-cohort NBS master script is required here.
