# Status

Control FC and graph analysis are both fixed to **cormat v6**.

The primary graph input therefore uses the normal, unsmoothed,
wavelet-despiked FC variant and the combined-hemisphere 52-node atlas.

The DVARS-based v7 variant is not used.
