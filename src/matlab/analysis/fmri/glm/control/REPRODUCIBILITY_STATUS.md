# Status

The primary control voxelwise GLM is configured as **v22**, with no early/late
split of Block 3.

The model uses the 14-column v1 nuisance set and the primary s6
wavelet-despiked EPI branch.

Execution still requires the non-distributed processed control EPI and
processed protocol/event files.
